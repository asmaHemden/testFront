export class Product {
    constructor(public id: number, public baseprice: string, public brand: string, public  category: Date, public  delivery: Date,
                public details: string, public imageurl: string, public productmaterial: string, public productname: string) {
    }
}
